#include <iostream>
#include <stdlib.h>
#include <vector>

using namespace std;

vector<int> hybridSort(vector<int> &arr)
{
    // todo
}

vector<int> mergeSort(vector<int> &arr)
{
    // todo
}

int main()
{

    // generate datasets

    hybridSort();
    mergeSort();

    // plot number of key comparisons over different size n with fixed S

    // plot number of key comparisons over different size n with different S
    return 0;
}
